@model IEnumerable<PortalCOSIE.Domain.Entities.Permiso>
@{
    ViewBag.Title = "Permisos";
}

<div class="card shadow">
    <div class="card-header">
        <div class="d-flex align-items-center justify-content-between flex-wrap">
            <!-- Título a la izquierda -->
            <h6 class="mb-0 fw-bold">Gestión de permisos</h6>

            <!-- Contenedor alineado a la derecha y compacto -->
            <div class="d-flex align-items-center ms-auto gap-2">
                <a href="@Url.Action("Crear", "Permiso")" class="btn btn-primary btn-sm shadow-sm">
                    <i class="fas fa-plus me-2"></i>Nuevo
                </a>
            </div>
        </div>
    </div>

    <div class="card-body overflow-auto" style="height: calc(100vh - 150px);">
        <div class="container">

            <table id="tblPermiso" class="table table-bordered shadow display compact">
                <thead class="table-secondary">
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Vista)</th>
                        <th>@Html.DisplayNameFor(model => model.Accion)</th>
                        <th>@Html.DisplayNameFor(model => model.CreatedAt)</th>
                        <th>@Html.DisplayNameFor(model => model.CreatedBy)</th>
                        <th>@Html.DisplayNameFor(model => model.UpdatedAt)</th>
                        <th>@Html.DisplayNameFor(model => model.UpdatedBy)</th>
                        <th colspan="2"></th>
                    </tr>
                </thead>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Vista)</td>
                        <td>@Html.DisplayFor(modelItem => item.Accion)</td>
                        <td>@Html.DisplayFor(modelItem => item.CreatedAt)</td>
                        <td>@Html.DisplayFor(modelItem => item.CreatedBy)</td>
                        <td>@Html.DisplayFor(modelItem => item.UpdatedAt)</td>
                        <td>@Html.DisplayFor(modelItem => item.UpdatedBy)</td>
                        <td>
                            <a href="@Url.Action("Editar", new { id = item.Id })" class="btn btn-sm btn-primary d-inline-block">
                                <i class="fas fa-edit"></i>
                            </a>
                        </td>
                        <td>
                            <a href="@Url.Action("Eliminar", new { id = item.Id })" class="btn btn-sm btn-danger">
                                <i class="fas fa-trash-alt"></i>
                            </a>
                        </td>
                    </tr>
                }

            </table>

        </div>
    </div>
</div>


@section scripts {
    <script>
        $(document).ready(function () {
            $('#tblPermiso').DataTable({
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.7/i18n/es-MX.json'
                },
                paging: false,
                scrollCollapse: true, //Si hay poco contenido -> no scrollY
                scrollY: '50vh', //Scroll dinamico
                //scrollX: true //Scroll dinamico
            });
        });
    </script>
}